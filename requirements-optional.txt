# Dependencias opcionales del ecosistema (busqueda vectorial, REQ-002).
# Sin ellas todo funciona con stdlib (indice JSON TF-IDF).
# Instalacion SOLO en .venv del proyecto y bajo confirmacion (P0.5):
#   bash scripts/setup.sh
# Versiones fijadas (P0.18): reproducibilidad de la cadena de suministro.
chromadb==1.5.9
sentence-transformers==6.1.0

# Cliente Jev AI liviano con llama.cpp (REQ-011).
# Requiere modelo GGUF (~3 GB); descargar con:
#   python3 scripts/download_jev_model.py
llama-cpp-python==0.3.35
