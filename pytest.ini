[pytest]
# Solo la suite real de tests/. Los scripts scripts/test_*.py son herramientas
# manuales (p. ej. test_determinism.py invoca opencode y gasta tokens, P0.19)
# y no deben recolectarse como tests ni en CI ni en local.
testpaths = tests
