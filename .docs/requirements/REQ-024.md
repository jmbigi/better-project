---
id: REQ-024
titulo: Dashboard de KPIs del framework en CI
estado: Draft
prioridad: Alta
version: 1.0
fecha_creacion: 2026-09-20
---

# REQ-024: KPIs del framework

> **Estado corregido a Draft (21-09-2026)**: no existe `docs/health.md` ni un
> generador de KPIs; solo habia un comentario `# REQ-024` en `scripts/ci.sh`.
> El estado `Implementado` era incorrecto (P0.1). Ver leccion LSN-036.

## Contexto
No hay KPIs medibles del proyecto como sistema. No se sabe si se mejora o empeora. Se necesitan 5 KPIs basicos publicados en cada CI.

## Criterios de aceptacion
- [ ] `docs/health.md` generado automaticamente en CI con 5 KPIs:
  1. Tiempo de onboarding (minutos desde clone hasta `verificar-proyecto.sh` verde)
  2. % REQs trazados (Implementados con refs / total no Deprecados)
  3. Mutation score global (ponderado)
  4. Tiempo CI completo (segundos)
  5. Coste mantenimiento (% tiempo en tooling vs producto)
- [ ] CI publica el dashboard como artifact o en `docs/health.md` versionado
- [ ] Metas numericas definidas para cada KPI
- [ ] Tendencia historica visible (ultimos 10 runs)

## Metrica de exito
Dashboard actualizado en cada CI. 5 KPIs con valor actual, meta y tendencia.

## Justificacion
Lo que no se mide, no se mejora. Hoy no hay KPIs del proyecto como sistema.